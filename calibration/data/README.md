# data

这里放数据集，而不是模型代码。

目录含义：

```text
raw/        原始数据，例如完整仿真输出或实测数据
processed/  处理后的数据，例如指标表、对齐后的时间序列、精简数据集
external/   外部导入的数据
```

建议：

```text
能从脚本重新生成的大文件优先放 results/
长期要复用的数据表可以放 data/processed/
手动导入或实物采集的数据可以放 data/external/
```

除非数据很小且确实需要复现，否则不要把大型 `.mat`、视频或大量图片提交到版本控制。
